// SPDX-License-Identifier: GPL-2.0-or-later

//! Codec bitstream knowledge the server needs, as pure functions over bytes, so
//! it is tested on the Linux host rather than only in Windows CI.

pub mod av1;
